<!DOCTYPE html>
<html>
  <head>
    <title>Alphablog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <nav style="background-color: silver; black: white; padding: 10px; display: flex; justify-content: space-between; align-items: center;">
      <h1><a href="/">Alphablog</a></h1>
      <div>
        <ul style="list-style: none; text-decoration: none">
          <li style="display: flex; gap: 20px">
            <%= link_to "Articles", articles_path %>
            <%= link_to "Users", users_path %>
          </li>
        </ul>
      </div>

      <div>
        <ul style="list-style: none; text-decoration: none">
          <% if logged_in? %>
            <li>
              <%= link_to current_user.username, user_path(current_user) %>
            </li>
            <li>
            <%= link_to 'Logout', logout_path, method: :delete, onclick: "return confirm('Are you sure?')" %>
            </li>
          <% else %>
            <li>
              <%= link_to "Log in", login_path %>
            </li>
            <li>
              <%= link_to "Sign in", signup_path %>
            </li>
          <% end %>
        </ul>
      </div>
    </nav>
    <%= render 'layouts/messages' %>
    <%= yield %>
  </body>
</html>
