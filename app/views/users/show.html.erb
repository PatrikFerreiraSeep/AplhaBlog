<h1><%= @user.username %>'s profile</h1>

<%= render 'articles/article' %>



<% if current_user.admin? %>
    <%= button_to 'Delete user', user_path(@user), method: :delete, onclick: "return confirm('Are you sure?')" %>
<% end %>
